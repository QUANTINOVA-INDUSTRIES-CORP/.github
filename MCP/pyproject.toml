[project]
name = "qic-mcp-control-plane"
version = "0.1.0"
description = "Canonical QUANTINOVA INDUSTRIES CORP MCP control plane"
requires-python = ">=3.11"
dependencies = [
  "httpx>=0.27,<1",
  "mcp[cli]>=1.28,<2",
]

[tool.uv]
package = false
